[build]
  publish = "public"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  META_INSTAGRAM_API_VERSION = "v26.0"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Beta signup (form name: beta-signup)
# Submissions: Netlify UI → Site → Forms → beta-signup
# Email notifications (verified 2026-07-23):
#   - feedback@macksims.com (site-wide submission_created)
#   - support@macksims.com (beta-signup submission_created)
# Mailto backup in public/js/site.js if the POST fails.
