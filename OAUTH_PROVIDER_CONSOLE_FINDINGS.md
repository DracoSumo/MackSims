# OAuth provider-console findings

Inspection date: 2026-07-29 (updated during Meta/Facebook login pass)

## Provider-console access result

`cursor-ide-browser` was briefly available and the authenticated Meta for Developers
**All apps** page was inspected while locked. Supabase Auth provider consoles were
not successfully opened before browser control dropped again. Google Cloud / GitHub
consoles were present as open tabs earlier in the session but were not used for this
Meta-focused pass. No secrets were retrieved, displayed, or stored. No Meta app was
deleted, rotated, or switched to Live. No App Review was submitted.

## Meta apps observed (non-secret App IDs only)

| Console display name | App ID | Mode | Role | Action taken |
| --- | --- | --- | --- | --- |
| Shutterbid | `956207094120610` | Development | Administrator | Inspected list only. **Left untouched.** This ID is the value currently configured in FishCrew for Instagram profile connection. |
| Fishcrew | `1709471443573822` | Development | Administrator | Inspected list only. Not remapped. Not used to enable FishCrew Facebook Login in this pass. |

No Meta apps named CoachCore, CurbCue, FairShare, MotoCrew, ThrottleLink, or Sermon
Studio were present. Dedicated Facebook Login apps for those four products still need
to be created.

## Repository-derived Google expectations

These are configuration expectations from application code and repository docs, not claims about current console state.

All four active Google login implementations use Supabase Auth. The browser returns to each app's `/auth/callback` route, but the Google OAuth client must redirect to the corresponding Supabase broker callback:

| App | Supabase project | Google authorized redirect URI required | Browser return URL generated by code |
| --- | --- | --- | --- |
| CoachCore | `bfqfbkldxbojrrxeidcc` | `https://bfqfbkldxbojrrxeidcc.supabase.co/auth/v1/callback` | `${window.location.origin}/auth/callback/` |
| CurbCue / FairShare | `dsbwqxhqktzsdleeobbi` | `https://dsbwqxhqktzsdleeobbi.supabase.co/auth/v1/callback` | `${window.location.origin}/auth/callback` |
| MotoCrew / ThrottleLink | `npmiwnxnqgonnmwvblyi` | `https://npmiwnxnqgonnmwvblyi.supabase.co/auth/v1/callback` | `${window.location.origin}/auth/callback` |
| Sermon Studio | `zipxwqkmenapnckwyzrh` | `https://zipxwqkmenapnckwyzrh.supabase.co/auth/v1/callback` | `${window.location.origin}/auth/callback` |

Google authorized JavaScript origins are not used by these `signInWithOAuth` browser calls directly; Supabase brokers the provider exchange.

CoachCore repository evidence is more specific:

- Google login is documented as enabled in Google Cloud project `nutt-362500`.
- The non-secret OAuth client identifier is documented only as prefix `337788492234-…`; the full value was not read or recorded.
- Google Calendar API and `calendar.events.readonly` are documented as enabled on the same OAuth client.
- The consent audience is documented as **Testing**, so only listed test users can grant the Calendar scope. Publication/verification status remains console-unconfirmed.

## Repository-derived Meta expectations

FishCrew remains the only app with a live Meta Instagram OAuth path:

- Configured non-secret Meta app ID: `956207094120610` (console label: Shutterbid).
- Configured app domain/canonical origin: `fishcrew.macksims.com`.
- Configured Meta redirect URI for Instagram: `https://fishcrew.macksims.com/`.
- The enabled Meta flow is an Instagram profile connection, not login.
- Google login and Facebook login stay explicitly disabled (`ENABLE_GOOGLE_AUTH: false`, `ENABLE_FACEBOOK_AUTH: false`).
- The four TypeScript apps now include Facebook in their OAuth provider unions and UI,
  gated by `*_ENABLE_FACEBOOK_AUTH` (default false) so production cannot start a broken
  Facebook login until Meta + Supabase Facebook are configured and tested.
- Stable policy URLs: `https://macksims-public-site.netlify.app/privacy/` and
  `https://macksims-public-site.netlify.app/account-deletion/`.

## Manual follow-up required

1. Create dedicated Meta Facebook Login apps for CoachCore, CurbCue, MotoCrew, and Sermon Studio (Development mode).
2. For each, set only that product’s exact Supabase `/auth/v1/callback`, plus domain/privacy/deletion URLs.
3. Enable the matching Supabase Facebook provider with App ID + secret (never store secrets in repo).
4. Test, then flip each product’s `*_ENABLE_FACEBOOK_AUTH` and redeploy.
5. For FishCrew login later: inspect `1709471443573822` carefully; do not touch Instagram settings on `956207094120610`.

No Meta/Supabase Facebook provider setting was saved in this pass after browser control dropped.
